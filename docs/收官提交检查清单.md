# 收官提交检查清单（A09）

> 用于最终提交前逐项打钩，避免“结果在、材料缺”或“口径不一致”。

## 1. 模型与指标产物

- [ ] `eddy` 最终权重与指标已锁定（`best.pt`、`metrics_summary_val.json`）
- [ ] `hydro` 最终权重与指标已锁定（`best.pt`、`metrics_summary_{val,test}.json`）
- [ ] `anomaly` 已完成正式一轮（建议命题方年份划分）并写出 `metrics_summary_{val,test}.json`
- [ ] `anomaly` 元数据已核查 `wind_only_files/wave_only_files/files_skipped`，并在材料中说明数据异构与降级策略
- [ ] 所有指标文件字段完整：`module`、`level`、`metrics`、`passed`、`tags`

## 2. 材料链路

- [ ] 已执行 `python scripts/export_material_table.py`
- [ ] `submission/tables/system_metrics_table.{md,csv}` 与最新结果一致
- [ ] 企业材料②所用图均存在（推荐从 `submission/figures/eddy_latest/`、`AutoDL/outputs/hydro/figures/` 选）
- [ ] 企业材料②中已写口径说明：eddy 当前为 `seg mAP@0.5`，与命题方 IoU 口径人工核对

## 3. 文档一致性

- [ ] `开发阶段文档/训练与实验记录.md` 已补本轮关键实验（参数、样本规模、指标、路径）
- [ ] `开发阶段文档/阶段二-开发记录.md` 已更新“当前推荐提交版本”
- [ ] `README.md` 与 `docs/` 中命令和路径可复制执行

## 4. 演示准备

- [ ] 固定演示顺序：eddy → hydro → anomaly
- [ ] 每模块至少 1 张效果图 + 1 条指标
- [ ] 演示文件路径固定（避免答辩时临场查找）

## 5. Git 与交付边界

- [ ] 只提交代码/配置/表格，不提交大体积数据与权重
- [ ] `submission/figures/` 如为本地材料目录，确认是否提交（默认可不提交）
- [ ] 推送前 `git status` 清晰，提交信息可追溯

