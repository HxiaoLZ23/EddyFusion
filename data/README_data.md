# 数据说明

命题方数据集位于仓库根目录 **`服创数据集/`**（与 `config/data.yaml` 中 `paths.raw_root` 一致）。若需改用 `data/raw/`，请同步修改配置并更新本说明。预处理后产物写入 `data/processed/` 下 `eddy/`、`hydro/`、`anomaly/`。

变量名、时空范围与划分方式见根目录 `config/data.yaml` 与《A09-项目开发文档》第四节；更新时同步修改本说明。
