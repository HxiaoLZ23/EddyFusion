# 命题方 NetCDF → 水文内部通道（与 config/hydro_hycom.yaml 中 input_features 顺序一致）
source_dataset: "海域要素预测"
source_format: "HYCOM NetCDF"

# 每个内部名对应 NetCDF 中可能的数据变量名（按优先顺序尝试）
channels:
  temp: [sst]
  sal: [sss, sss]
  u: [ssu]
  v: [ssv]

dimensions_example:
  time: 12
  lat: 138
  lon: 125
