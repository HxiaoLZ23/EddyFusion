---
description: A09 海洋智能分析项目全局 AI 开发规范（与 AI_DEV_REQUIREMENTS.md 一致）
alwaysApply: true
---

# A09 全局开发规范

接手或继续开发前，**必须完整阅读**项目内规范原文：`相关文件/AI_DEV_REQUIREMENTS.md`。本规则为摘要；冲突时以该文件及《A09-最终技术方案与降级策略》为准。

## 阶段文档（`开发阶段文档/`）

- 每完成阶段〇～六之一，**新增或更新**该阶段项目开发文档；目录建议 `.gitignore`，不推远程。
- 文档须让另一 AI **读完即可接手**：目标与完成项、目录与入口、依赖与环境、从零跑通步骤、下一阶段计划。
- 必须含 **「不可更改」**：config 的 `level` 含义、eval 输出字段与指标汇总约定、数据层路径与形状、**PyTorch≥2.5.1** 等；不得擅自破坏 `run_eval_all.sh` / 企业材料指标链。
- 必须含 **「需人工完成」**：命题方数据获取、云端 GPU 训练、台风等公开数据下载、演示与提交材料、与命题方对接评测口径等。

## 依赖

- 增/换任何第三方包须**同步更新**根目录 **`requirements.txt`**（版本与注释）；禁止文档或代码默认“已装某包”而未写入。
- 赛题要求：**NetCDF4、xarray、PyTorch≥2.5.1** 须在 requirements 中显式约束。

## 仓库与目录

- 代码根目录与 **`A09-ocean-ai/`** 约定一致（若在 `创赛/` 下，在阶段文档写明实际根路径）；保持 `config/`、`data/`、`src/`、`scripts/`、`outputs/` 等相对关系。
- 勿将 `开发阶段文档/`、大体积数据与权重、提交用视频与敏感信息提交远程（或仅占位说明）。

## 训练与实验

- 有训练则更新 **`开发阶段文档/训练与实验记录.md`**（或阶段文档内对应小节）：实验 ID、脚本与 config、数据、超参、环境、产物路径、最佳指标、可复现命令；命名参见规范第 5 节。
- 数据集规模变更时维护 **`开发阶段文档/数据集说明_论文用.md`**（若使用）。

## 每阶段结束自检

对照 `AI_DEV_REQUIREMENTS.md` 第 6 节小结清单逐项勾选。
